---
icon: fontawesome/brands/python
---

# Gather, scatter & indexing

[`XTensor`][fiery.xtensor.XTensor] methods that select, split, or scatter
along named dimensions — coordinate-aware `sel`/`isel`/`interp`, PyTorch's
gather/scatter family, and slicing/splitting.

::: fiery.xtensor.XTensor
    options:
      show_root_heading: false
      show_root_toc_entry: false
      members:
        - sel
        - isel
        - interp
        - gather
        - index_add
        - index_copy
        - index_fill
        - index_select
        - masked_select
        - nonzero
        - scatter
        - scatter_add
        - take_along_dim
        - where
        - chunk
        - flip
        - narrow
        - roll
        - select
        - split
        - unbind
