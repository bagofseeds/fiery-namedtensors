---
icon: fontawesome/brands/python
---

# Options

Library-wide behaviour switches (e.g. the coordinate/unit backend), set
globally, scoped with a `with` block, or applied as a decorator.

::: fiery.xtensor.set_options
